import React from 'react';
import PropTypes from 'prop-types';

const DisabledPlaceholder = ({ width }) => (
  <div></div>
);

DisabledPlaceholder.prpoTypes = {
  width: PropTypes.number,
};

export default DisabledPlaceholder;
